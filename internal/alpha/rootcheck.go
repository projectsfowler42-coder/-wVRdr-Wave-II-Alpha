// Package alpha provides a minimal root package so repository-level Go
// integrity commands such as `go test ./...` and `go vet ./...` have a
// package to inspect from the Alpha root while MDK remains a separate module.
package alpha

const ScaffoldName = "wvrdr-wave-ii-alpha"
