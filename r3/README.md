# ~wVRdr~ R³ Engine Spine

Status: Wave-II~Alpha implementation track.

This folder is the active R³ build spine. It is not a handoff folder and not a parking lot.

## Purpose

Move ~wVRdr~ toward the intended Sim-to-Win / PreCog / Prop Group / historical pattern discovery engine as quickly as possible while preserving hard truth boundaries.

## Active modules

- `truth.ts` — Truth envelope and promotion blocking.
- `propDoctrine.ts` — harvested prop mode/play/universe doctrine.
- `circuitBreakers.ts` — Hard Deck, Volatility Clamp, Ratchet.
- `survivorshipInversion.ts` — returning-plane/non-returning-plane lifecycle layer.
- `promotionGate.ts` — blocks fake LIVE, fake confidence, and premature EDGE labels.
- `index.ts` — exports.

## Meaning of staged/guarded code

The guardrails do not mean abandoned or delayed. They mean the engine can advance without accidentally:

- placing broker orders
- creating fake LIVE truth
- contaminating Wave-I cockpit state
- promoting unvalidated rules
- hiding failed hypotheses

## Promotion sequence

1. Encode doctrine as pure modules.
2. Add deterministic tests.
3. Connect imported intelligence corpus as source material.
4. Promote only reviewed modules into active Wave-II services.
5. Keep Wave-I cockpit protected until the R³ truth contract is proven.
